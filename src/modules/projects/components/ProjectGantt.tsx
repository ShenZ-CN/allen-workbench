import type{ProjectOverview,ProjectStage}from"@/lib/types";import{Badge}from"@/components/ui/badge";
const stages:ProjectStage[]=["RFQ","Quotation","Technical Review","Sample","PPAP","SOP","Mass Production"];
export function ProjectGantt({projects}:{projects:ProjectOverview[]}){return <div className="overflow-auto"><table className="data-table min-w-[950px]"><thead><tr><th>项目</th>{stages.map(s=><th key={s}>{s}</th>)}</tr></thead><tbody>{projects.map(p=>{const current=stages.indexOf(p.current_stage);return <tr key={p.id}><td><b>{p.project_name}</b><div className="text-xs text-slate-500">{p.customer_name}</div></td>{stages.map((s,i)=><td key={s}><span className={`block h-2 min-w-20 rounded-full ${i<current?"bg-emerald-400":i===current?(p.delay_status==="delayed"?"bg-rose-500":"bg-blue-500"):"bg-slate-200"}`}/>{i===current&&<div className="mt-1"><Badge tone={p.delay_status==="delayed"?"red":"blue"}>当前</Badge></div>}</td>)}</tr>})}</tbody></table></div>}
