import{Badge}from"@/components/ui/badge";import{Card,CardHeader}from"@/components/ui/card";import{EmptyState,ErrorState,LoadingState}from"@/components/shared/states";import{MetricCard}from"@/components/shared/metric-card";import{currency,number}from"@/lib/utils";import{useProduction}from"@/modules/production/hooks/use-production";
export function ProductionPage(){const{data,isLoading,error}=useProduction();if(isLoading)return<LoadingState/>;if(error)return<ErrorState error={error}/>;if(!data?.length)return<EmptyState title="尚无量产台账" description="项目完成 SOP 后，可按项目与零件建立月度量产经营记录。"/>;const sum=data.reduce((a,x)=>({f:a.f+x.forecast_sales,r:a.r+x.actual_revenue,p:a.p+x.actual_profit}),{f:0,r:0,p:0});return<div className="space-y-4"><section className="grid gap-4 md:grid-cols-3"><MetricCard label="销售预测" value={currency(sum.f)}/><MetricCard label="实际收入" value={currency(sum.r)}/><MetricCard label="年度利润" value={currency(sum.p)}/></section><Card><CardHeader title="SOP 后量产经营" subtitle="订单 · 交付 · 质量 · Revenue"/><div className="overflow-auto"><table className="data-table"><thead><tr><th>项目 / 零件</th><th>月份</th><th>预测销售</th><th>实际收入</th><th>利润</th><th>交付</th><th>质量</th><th>PPM</th></tr></thead><tbody>{data.map(x=><tr key={x.id}><td><b>{x.project_name}</b><div className="text-xs text-slate-500">{x.customer_name} · {x.part_number}</div></td><td>{x.period}</td><td>{currency(x.forecast_sales)}</td><td>{currency(x.actual_revenue)}</td><td>{currency(x.actual_profit)}</td><td><Badge tone={x.delivery_status==="delayed"?"red":x.delivery_status==="risk"?"amber":"green"}>{x.delivery_status}</Badge></td><td><Badge tone={x.quality_status==="risk"?"red":x.quality_status==="attention"?"amber":"green"}>{x.quality_status}</Badge></td><td>{number(x.ppm)}</td></tr>)}</tbody></table></div></Card></div>}
