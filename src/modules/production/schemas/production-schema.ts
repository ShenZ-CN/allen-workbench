import{z}from"zod";export const productionSchema=z.object({period:z.string().regex(/^\d{4}-\d{2}$/),forecast_quantity:z.number().nonnegative(),order_quantity:z.number().nonnegative(),due_quantity:z.number().nonnegative(),delivered_quantity:z.number().nonnegative(),ppm:z.number().nonnegative(),customer_complaints:z.number().int().nonnegative(),return_scrap_quantity:z.number().nonnegative(),actual_unit_price:z.number().nonnegative(),actual_unit_cost:z.number().nonnegative()});
